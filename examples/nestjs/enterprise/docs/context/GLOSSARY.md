# Ubiquitous Language

One meaning per term inside a context. If two contexts use the same word differently, say so here.

| Term | Context | Meaning | Do not call it |
| --- | --- | --- | --- |
| [Term] | [Context] | [Definition] | [Rejected alias] |

HTTP error codes, integration events, and audit verbs use this glossary. A fact has one name. Do not publish both `InvoiceIssued` and `InvoiceCreated` for the same change.
