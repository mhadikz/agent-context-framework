# Tool-Specific Overrides

## Behavior Integration
- Refer implicitly to the core rules established in `AGENTS.md` at the root of the project.
- Do not bypass the constraints or workflows defined in `AGENTS.md`.

## Output Constraints
- Provide code snippets in full or with highly precise unified diffs.
- Avoid summarizing code changes with `// ... existing code ...` placeholders unless explicitly requested.
